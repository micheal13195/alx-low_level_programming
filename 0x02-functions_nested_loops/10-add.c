#include "main.h"

/**
 * add - this function add two integers
 * @k: this is one of the integer parameter
 * @l: this is the second operand, also an ingeger
 * Return: this function return the sum of the
 * two integers
 */
int add(int k, int l)
{
	return (k + l);
}
