#include <stdio.h>

/**
 * main - all single numbers in base 10
 *
 * Return: 0 at the end of base 10
 *
 */

int main(void)

{
	int ch;

	for (ch = 48, ch <= 57; ch++;)
	{
	putchar(ch);
	}
	putchar(10);

	return (0);
}
