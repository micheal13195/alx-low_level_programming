#include "main.h"
#include <stdio.h>

/**
 * main - this function prints the name of the program
 * @argc: this denotes the number of command line arguments
 * @argv: this is an array name
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
