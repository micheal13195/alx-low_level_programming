#include "main.h"

/**
 * print_line - function to print lines btw two point
 * @n: distance between two straigth line
 * Return: distance between two point
 */

void print_line(int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		_putchar(95);
	}
		_putchar('\n');
}
