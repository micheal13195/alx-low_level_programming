#include <stdio.h>

/**
 * mul - this function multiplies two numbers
 * @a: this function accepts a as a parameter
 * @b: this function accepts b also as a parameter
 * Return: this function returns the product of the
 * two operands
 */
int mul(int a, int b)
{
	return (a * b);
}
