#include <stdio.h>
#include "main.h"

/**
 * _isupper(int c) - function that check upper case
 *@c: Case character
 * Return: 0
 */

int _isupper(int c)
{
	char c

	if (c >= 'A', c <= 'Z', c++)
	{
	printf("print is upper case")
	;
	return (1);
	}
	else
	printf("print is lowercae")
	;
	return (0);
}
